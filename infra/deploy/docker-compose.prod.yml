###############################################################################
# Production Docker Compose — Single EC2 deployment
# Runs: PostgreSQL, Redis, API server, Signaling server, coturn, Caddy (HTTPS)
# Domain: epbx.negd.in
# Cost: ~₹3,500–4,500/month on t3.medium
###############################################################################
name: hotel-app-prod

services:
  # ─── PostgreSQL 15 ──────────────────────────────────────────────────────────
  postgres:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - backend

  # ─── Redis 7 ────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    restart: always
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - backend

  # ─── API Server (NestJS) ────────────────────────────────────────────────────
  api-server:
    build:
      context: ../..
      dockerfile: infra/deploy/Dockerfile.api
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file: .env.prod
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      REDIS_URL: redis://redis:6379
      PORT: "3001"
    networks:
      - backend

  # ─── Signaling Server (Socket.io) ──────────────────────────────────────────
  signaling-server:
    build:
      context: ../..
      dockerfile: infra/deploy/Dockerfile.signaling
    restart: always
    depends_on:
      redis:
        condition: service_healthy
    env_file: .env.prod
    environment:
      REDIS_URL: redis://redis:6379
      API_SERVER_URL: http://api-server:3001
      PORT: "3002"
    networks:
      - backend

  # ─── coturn (STUN/TURN) — host network for UDP relay ───────────────────────
  coturn:
    image: coturn/coturn:latest
    restart: always
    network_mode: host
    volumes:
      - ./coturn/turnserver.prod.conf:/etc/coturn/turnserver.conf:ro
    command: -c /etc/coturn/turnserver.conf

  # ─── Caddy (reverse proxy + auto HTTPS via Let's Encrypt) ──────────────────
  caddy:
    image: caddy:2-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - api-server
      - signaling-server
    networks:
      - backend

volumes:
  postgres_data:
  redis_data:
  caddy_data:
  caddy_config:

networks:
  backend:
    driver: bridge
